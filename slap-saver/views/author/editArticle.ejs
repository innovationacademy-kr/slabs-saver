<!DOCTYPE html>
<html>

<head>
  <title>
    <%= title %>
  </title>
</head>

<body>
  <div class="side-nav">
    <button class="side-nav__logout">로그아웃</button>
    <a href="/author/edit-meeting">편집 회의</a>
    <a href="/author/articles/new">기사 작성</a>
    <a href="/author/articles">내 기사 목록</a>
  </div>
  <form method="post" class="edit-article" action=<%=article.id %> enctype="multipart/form-data">
    <div class="edit-article__input">
      <label for="category">카테고리</label>
      <select name="category" id="category">
        <option value=2 <%= article.category === 2 ? 'selected' : '' %> >정치</option>
        <option value=3 <%= article.category === 3 ? 'selected' : '' %> >경제</option>
        <option value=4 <%= article.category === 4 ? 'selected' : '' %> >기술</option>
        <option value=5 <%= article.category === 5 ? 'selected' : '' %> >환경</option>
        <option value=6 <%= article.category === 6 ? 'selected' : '' %> >과학</option>
        <option value=7 <%= article.category === 7 ? 'selected' : '' %> >건강</option>
        <option value=8 <%= article.category === 8 ? 'selected' : '' %> >스포츠</option>
      </select>
        <div class="edit-article__input--default">
          <label for="headline">헤드라인</label>
          <input type="text" id="headline" name="headline" value='<%= article.headline %>' placeholder="뉴스의 헤드라인을 입력해주세요"
          maxlength="30" />
          <br />
          <div>
            <label for="current-picture">현재 사진</label>
            <img src=<%=article.image %> alt="current-picture" width="300px" height="300px">
            <label for="picture">사진</label>
            <input type="file" id="picture" name="picture" accept="image/gif, image/jpeg, image/png" />
            <br />
            <label for="imageDesc">사진 설명</label>
            <input type="text" id="imageDesc" name="imageDesc" value='<%=article.imageDesc %>' placeholder="사진에 대한 설명을
            적어주세요" />
            <label for="imageFrom">사진 출처</label>
            <input type="text" id="imageFrom" name="imageFrom" value='<%=article.imageFrom %>' placeholder="사진에 대한 출처를 적어주세요" />
          </div>
          <label for="breifing">100자 브리핑</label>
          <textarea id="briefing" name="briefing" placeholder="뉴스에 대한 브리핑을 작성해주세요"
          maxlength="30"> <%=article.briefing %>  </textarea>
        </div>
        <select name="paragraph-select" class="paragraph-select">
            <option value='포인트'>포인트</option>
            <option value='향후예측'>향후예측</option>
            <option value='논쟁점'>논쟁점</option>
            <option value='직접입력(더블클릭으로 수정)'>직접입력</option>
        </select>
      <button type="button" class="paragraph-list--Btn">소단락 추가</button>
      <div class="paragraph-list">
        <% if (paragraphs?.length > 0) { %>
          <% paragraphs.forEach((p)=> { %>
          <div class="paragraph-list__item">
            <label for=paragraph-title ></label>
            <input id=paragraph-title name=paragraph-title class="paragraph-item__title" value= <%= p[0] %> >
            <label for=paragraph-conetent ></label>
            <textarea id=paragraph-content name=paragraph-content class="paragraph-item__content" type="text" value=<%= p[1] %> ><%= p[1] %></textarea>
          </div>
          <% }) %>
        <% } %>
      </div>
    </div>
    <button class="edit-article--save" name="saveBtn" type="submit">저장</button>
    <button class="edit-article--complete" name="completeBtn" type="submit">완료</button>
  </form>
  <script src="/javascripts/addSection.js"></script>
</body>

</html>


</html>